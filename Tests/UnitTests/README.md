This directory implements unit tests for the source code. You can run all tests from within this directory via

python RunTests.py

Note: You need to adjust the model loader in the runconfig.yaml to reflect the model setup you want to use (MACE or NequIP).
